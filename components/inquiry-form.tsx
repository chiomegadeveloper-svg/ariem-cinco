"use client";
import { useState } from "react";
import { Send } from "lucide-react";
import { getSupabase } from "@/lib/supabase";
export function InquiryForm(){const [status,setStatus]=useState<"idle"|"sending"|"sent"|"error">("idle");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setStatus("sending");const form=event.currentTarget;try{const supabase=getSupabase();if(!supabase)throw new Error();const payload=Object.fromEntries(new FormData(form).entries());const{error}=await supabase.from("inquiries").insert(payload);if(error)throw error;form.reset();setStatus("sent")}catch{setStatus("error")}}return <form className="inquiry-form" onSubmit={submit}><label>Full name<input name="name" required placeholder="Your name"/></label><label>Email address<input name="email" type="email" required placeholder="name@organization.com"/></label><label>Organization<input name="organization" placeholder="Organization or school"/></label><label>Event type<select name="event_type" defaultValue=""><option value="" disabled>Select event type</option><option>Conference keynote</option><option>Workshop</option><option>Leadership session</option><option>School engagement</option><option>Other</option></select></label><label>Preferred date<input name="preferred_date" type="date"/></label><label>Audience size<input name="audience_size" type="number" min="1" placeholder="Estimated attendees"/></label><label className="full">Message<textarea name="message" required rows={5} placeholder="Tell us about the audience, goals, location, and format."/></label><button className="btn btn-primary full" disabled={status==="sending"}>{status==="sending"?"Sending…":<><Send size={17}/>Send request</>}</button>{status==="sent"&&<p className="form-note success">Thank you. Your request has been received.</p>}{status==="error"&&<p className="form-note error">Connect Supabase to activate requests.</p>}</form>}
